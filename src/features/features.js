import {createSlice} from '@reduxjs/toolkit'







const counterSlice = createSlice({
    name: 'auth',
    initialState: { value: 0 },           //потом заменить!
    
    reducers: {

    }

});

export default counterSlice.reducer;